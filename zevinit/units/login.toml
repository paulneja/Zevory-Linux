[unit]
description = "Console login"
requires = ["devtmpfs"]
wants = ["zevlog"]
after = ["devtmpfs", "zevlog"]
conflicts = ["rescue"]

[service]
kind = "simple"
start = "/sbin/getty 38400 tty1"
restart = "always"
restart_delay = 1
restart_limit = 10
directory = "/"
