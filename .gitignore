# generated stuff, all of it out of tree
/build/
/sources/
*.img
*.iso
*.qcow2

# rust. target is generated, the lock is not: zevinit is a binary and we pin its
# deps the same way we pin the kernel and busybox
/target/

# editor / os
.vscode/
.idea/
*.swp
.DS_Store

# never commit these
*.local.toml
.env
